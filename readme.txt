// show seams bonus

1. create a duplicate of the working image

2. reduceImageWidth ( ); 

3. We then iterate the seams and for each seam value at index J we set the RGB using seamColor in order to show it on the image